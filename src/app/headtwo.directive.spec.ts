import { HeadtwoDirective } from './headtwo.directive';

describe('HeadtwoDirective', () => {
  it('should create an instance', () => {
    const directive = new HeadtwoDirective();
    expect(directive).toBeTruthy();
  });
});
