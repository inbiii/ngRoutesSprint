import { SpanDirective } from './span.directive';

describe('SpanDirective', () => {
  it('should create an instance', () => {
    const directive = new SpanDirective();
    expect(directive).toBeTruthy();
  });
});
